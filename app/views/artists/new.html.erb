<h1>New Artist</h1>

<%= form_with model: @artist do |f| %>
  <p>
    <%= f.label :name, 'Name' %> <br />
    <%= f.text_field :name %>
  </p>
  <p><%= f.submit 'Create' %></p>
<% end %>

<p>
  <span class="symbol">❌</span> <%= link_to 'Cancel', artists_path %>
</p>